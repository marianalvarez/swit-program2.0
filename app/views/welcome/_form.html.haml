= form_for @welcom do |f|
  - if @welcom.errors.any?
    #error_explanation
      %h2= "#{pluralize(@welcom.errors.count, "error")} prohibited this welcom from being saved:"
      %ul
        - @welcom.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :username
    = f.text_field :username
  .field
    = f.label :password
    = f.text_field :password
  .actions
    = f.submit 'Save'
