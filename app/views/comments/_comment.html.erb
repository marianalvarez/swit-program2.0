  <hr>
  <div id="commentbody">
  <%= comment.body %>
 <div id="commented">Commented <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.commenter %>

<%= link_to 'Delete', [comment.article, comment], :remote => true,
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
 </div>

</div>
<hr>

