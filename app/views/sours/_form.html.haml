= form_for ([@article, @sour]) do |f|
  - if @sour.errors.any?
    #error_explanation
      %h2= "#{pluralize(@sour.errors.count, "error")} prohibited this sour from being saved:"
      %ul
        - @sour.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :commenter
    = f.text_field :commenter
  .field
    = f.label :article_id
    = f.number_field :article_id
  .actions
    = f.submit 'Save'
