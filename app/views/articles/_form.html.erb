<%= form_for @article do |f| %>
  <% if @article.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@article.errors.count, "error") %> prohibited
      this article from being saved:</h2>
    <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div id="article">
     <div class="small-12 columns">
  <p>
    <%= f.hidden_field :title, :value => current_user.username %>
  </p>
 
    <div id="create_swit">
    Create a Swit
  </div>
      <p>
    <%= f.text_area :text , :id => "txt", :maxlength => 140 %>
 Characters remaining: <span id="count"> 140</span>
  </p>

  
 <div class=""><%= f.submit "Submit", :class => 'button tiny push-7' %> </div>
    
</div>
</div>
<% end %>


